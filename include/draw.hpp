#pragma once
// #include "GLFW/glfw3.h" // GLFW
#include "defs.hpp"
#include <string>
#include <cstdlib>
#include <functional>
#include <windows.h>
#include <vector>

using namespace std;


class GContext
{
private:
		
public:
	drawPixel();
	GContext();
	GContext();
	
};

class Window {
public:

private:
	Window(GContext*);
	~Window();
};
