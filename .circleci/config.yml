version: 2
jobs:
  build:
    environment: #environment
    docker:
      - image: debian:stretch
      # - image: gcc
    steps:
#       - checkout
      - run:
          name: platform check
          command: uname -a
      - run: 
          name: test apt-get
          command: apt-get install g++
      - run:
          name: Compile
          command: g++ -v
      - run:
          name: End
          command: date
