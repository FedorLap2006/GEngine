// #define check_fail if(this->failure) return;

/*int main() {
	GLFWwindow *win;

	if (!glfwInit())
		exit(EXIT_FAILURE);
	win = glfwCreateWindow(640,480,"hello world", NULL, NULL);
	if (!win) {
		glfwTerminate();
		exit(EXIT_FAILURE);
	}

	glfwMakeContextCurrent(win);

	while(!glfwWindowShouldClose(win)) {
		glClear(GL_COLOR_BUFFER_BIT);

		glfwSwapBuffers(win);

		glfwPollEvents();
	}

	glfwTerminate();
	exit(EXIT_SUCCESS);
}*/




