language: cpp
compiler: gcc g++
sudo: true
# install:
#  - libxrandr
#  - libwrandr
os: windows
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      libxrandr2
      #libwrandr-dev
    
script:
  - cd build
  - git status
  # - cmake -DBUILD_SHARED_LIBS=OFF ..
  - g++ -v
